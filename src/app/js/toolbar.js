import React from 'react';
import Button from 'js/button';

class Toolbar extends React.Component {
  render() {
    return (
      <div>
        <Button buttonText="+ App" buttonCommand="" />
      </div>
    );
  }
}

export default Toolbar;
